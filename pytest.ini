[pytest]
minversion = 7.0
addopts = -ra
testpaths = tests
pythonpath = .
markers =
    functional: verifies a single command/feature in isolation
    integration: verifies multiple commands working together
    regression: guards against previously fixed bugs
    system: end-to-end tests validating overall instrument behavior